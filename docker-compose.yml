version: '3.8'

services:
  web:
    build: .
    ports:
      - "8000:8000"
    environment:
      - DEBUG=1
      - DATABASE_URL=sqlite:///db.sqlite3
    volumes:
      - .:/app
    command: python manage.py runserver 0.0.0.0:8000
